import ToolPage, { toolMetadata } from "@/components/ToolPage";

export const metadata = toolMetadata("json-to-json-schema");

export default function Page() {
  return <ToolPage id="json-to-json-schema" />;
}
