import ToolPage, { toolMetadata } from "@/components/ToolPage";

export const metadata = toolMetadata("json-to-typescript");

export default function Page() {
  return <ToolPage id="json-to-typescript" />;
}
