import ToolPage, { toolMetadata } from "@/components/ToolPage";

export const metadata = toolMetadata("json-schema-to-json");

export default function Page() {
  return <ToolPage id="json-schema-to-json" />;
}
