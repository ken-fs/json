import ToolPage, { toolMetadata } from "@/components/ToolPage";

export const metadata = toolMetadata("json-to-python");

export default function Page() {
  return <ToolPage id="json-to-python" />;
}
